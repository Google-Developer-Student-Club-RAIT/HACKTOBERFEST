#include<stdio.h>
int main()
{
  int l, b, area;
  printf("Enter lenght of your rectangle \n");
  scanf("%d",&l);
  printf("Enter breadth of your rectangle \n");
  scanf("%d",&b);
  area = l*b;
  printf("Area of the given rectangle is = %d",area);
  return 0;
}
  
## Area of Rectangle (Python Code) ##

breath = float(input("Enter the breath of the rectangle :- "))
length = float(input("Enter the length of the rectangle :- "))

area = breath*length

print("The area of the rectangle","is",area);
